version: '3.7'

services:
  web:
    build: .
    command: sh entrypoint.sh
    volumes:
      - .:/usr/src/app/
    ports:
      - 9000:8000
    env_file:
      - .env.dev
    depends_on:
      - dbd
  dbd:
    image: postgres:12.12-alpine
    restart: always
    ports:
      - "5433:5432"
    volumes:
      -     design_data:/var/lib/postgresql/data/
    env_file:
      - .env.dev.db

volumes:
    design_data: